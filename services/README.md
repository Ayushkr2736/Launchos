# LaunchOS Services

#

# Long-running workers and agent runtimes that are not part of the HTTP API

# or the desktop shell live here (e.g. agent executors, queue consumers,

# embedding indexers).

#

# Each service should be its own package under this directory and registered

# in pnpm-workspace.yaml when introduced.
