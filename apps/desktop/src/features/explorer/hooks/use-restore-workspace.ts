/**
 * After Zustand rehydrates persisted workspace state, restore the last
 * (or preferred) folder via Workspace Manager.
 */
export { useRestoreLastWorkspace as useRestoreWorkspace } from '@/modules/workspace-manager';
